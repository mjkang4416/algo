## **문제**

A,B 두 사람이 볼링을 치고 있습니다. 두 사람은 서로 무게가 다른 볼링공을 고르려고 합니다. 볼링공은 총 N개가 있으며 각 볼링공마다 무게가 적혀 있고, 공의 번호는 1번부터 순서대로 부여됩니다. 또한 같은 무게의 공이 여러 개 있을 수 있지만, 서로 다른 공으로 간주합니다. 볼링공의 무게는 1부터 M까지의 자연수 형태로 존재합니다.

예를 들어 N이 5이고, M이 3이며 각각의 무게가 차례대로 1,3,2,3,2일 때 각 공의 번호가 차례대로 1번부터 5번까지 부여됩니다. 이때 두 사람이 고를 수 있는 볼링공 번호의 조합을 구하면 다음과 같습니다.

> (1번, 2번), (1번, 3번), (1번, 4번), (1번, 5번), (2번, 3번), (2번, 5번), (3번, 4번), (4번, 5번)
> 

결과적으로 두 사람이 공을 고르는 경우의 수는 8가지입니다. N개의 공의 무게가 각각 주어질 때, 두 사람이 볼링공을 고르는 경우의 수를 구하는 프로그램을 작성하세요.

---

**입력 조건**

- 첫째 줄에 볼링공의 개수 N, 공의 최대 무게 M이 공백으로 구분되어 각각 자연수 형태로 주어집니다. (1<=N<=1,000 ,1<=M<=10)
- 둘째 줄에 각 볼링공의 무게 K가 공백으로 구분되어 순서대로 자연수 형태로 주어집니다. (1<=K<=M)

---

**출력 조건**

- 첫째 줄에 두 사람이 볼링공을 고르는 경우의 수를 출력합니다.

## 소감

```java

        for(int i =1; i<N+1; i++){
            arr[i] = sc.nextInt();
        }
```

얘가 입력값이 아니라 .. 랜덤으로 자동 입력 돼야 하는 줄 알고 … 여러가지방법을 쓰다가 시간이 걸렸던 문제다. 

문제 자체는 직관적이다. 

```java
//ArrayList<> 로 담지말고 인터페이스에 구현체 담자 !!

List<Integer> list = new ArrayList<>();
```

처음에는 이렇게 담고 셔플 시키려고 했는데  셔플은 걍 한번 list 를 섞는거고 계속 랜덤으로 N 까지 1,2,3..N 

중 하나가 나와야 하기 때문에 기각 but 인터페이스를 구현체에 담자 .. 라는 추상화의 교훈을 얻었다. 

이때까지 구현객체를 직접 쓰고 있었는데 코테 측면이 아니라 개발 측면에서 이러면 리펙토링이나 유지보수 할

때 문제가 생길 듯 하다. 

```java
Random random = new Random();

for(int i =1; i<N+1; i++){

arr[i] = random.nextInt(M)+1; //rand.nextInt 는 0~n-1 중 하나를 뽑아줌

System.*out*.print(arr[i]+" ");

}
```

다음방법 Random 객체를 하나 만들어서 받은  M 을 0~M-1 중 랜텀으로 가져오게 구현했다. 비록 이번 문제

는 입력받는 형식이지만 랜덤 값 자동으로 넣어줄 떄 요긴하게 쓸듯 

이번 문제 역시 그리디.. ? 라고 하기에는 그리디 같지가 않았다. for 문 두번 돌리는건 시간 복잡도를 너무 잡아

먹는 것 같은데 .. 크게 다른 방법은 없을 것 같다.
